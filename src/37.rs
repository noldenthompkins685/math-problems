fn main() {
    // Example Rust code: Addition assignment operator example

    let x = 5;
    let y = 3;

    // Example of using the addition assignment operator in a function
    let result = x + &y; // Equivalent to adding 'y' and 'x' using the += operator
}
